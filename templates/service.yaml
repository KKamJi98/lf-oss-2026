apiVersion: v1
kind: Service
metadata:
  name: {{ include "lf-oss-na-2026.fullname" . }}
  labels:
    {{- include "lf-oss-na-2026.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lf-oss-na-2026.selectorLabels" . | nindent 4 }}
